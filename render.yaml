services:
  - type: web
    name: line-chinese-tutor-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: OPENAI_MODEL
        value: gpt-5.4-mini
